#include <stdio.h>
#include <stdlib.h>
#include "index_first_zero.h"
#define max 100
int index_first_zero(int num[max],int length)
{
int i;
for (i = 0; i< length && num[i]!=0; i++)
    {
    }
return i;
}
